import { makeStyles } from "@mui/styles";

const styles = makeStyles({
   box: {
      display: 'flex',
   },
   shape: {
      padding: 20,
      backgroundColor: 'red',
      margin: 10
   }
});

export default styles;